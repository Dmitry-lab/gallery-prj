export type TImage = {
  id: string;
  author: string;
  url: string;
  isLiked: boolean;
};

export type TDetailImageInfo = {
  id: string;
  author: string;
  url: string;
  download_url: string;
  width: number;
  height: number;
}
